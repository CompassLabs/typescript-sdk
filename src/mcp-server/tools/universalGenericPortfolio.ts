/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { universalGenericPortfolio } from "../../funcs/universalGenericPortfolio.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1GenericPortfolioRequest$inboundSchema,
};

export const tool$universalGenericPortfolio: ToolDefinition<typeof args> = {
  name: "universal-generic-portfolio",
  description: `List User Portfolio

Fetch the detailed portfolio of a specific wallet address on a given blockchain.

This includes the total value of the portfolio in USD and a breakdown of token
balances, including their respective values and quantities.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await universalGenericPortfolio(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
