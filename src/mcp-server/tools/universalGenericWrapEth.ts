/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { universalGenericWrapEth } from "../../funcs/universalGenericWrapEth.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.WrapEthRequest$inboundSchema,
};

export const tool$universalGenericWrapEth: ToolDefinition<typeof args> = {
  name: "universal-generic-wrap-eth",
  description: `Wrap ETH

Wrapping ETH creates an ERC20 compliant form of ETH that is typically needed for
it to be traded on DeFi protocols.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await universalGenericWrapEth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
