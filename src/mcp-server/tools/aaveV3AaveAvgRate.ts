/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aaveV3AaveAvgRate } from "../../funcs/aaveV3AaveAvgRate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1AaveAvgRateRequest$inboundSchema,
};

export const tool$aaveV3AaveAvgRate: ToolDefinition<typeof args> = {
  name: "aave-v3-aave-avg-rate",
  description: `Interest Rates: Time Average

Provides time-weighted averages of deposit and borrow rates for Aave reserves.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aaveV3AaveAvgRate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
