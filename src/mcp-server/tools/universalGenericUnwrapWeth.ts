/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { universalGenericUnwrapWeth } from "../../funcs/universalGenericUnwrapWeth.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.UnwrapWethRequest$inboundSchema,
};

export const tool$universalGenericUnwrapWeth: ToolDefinition<typeof args> = {
  name: "universal-generic-unwrap-weth",
  description: `Unwrap WETH

Unwrapping WETH converts the ERC-20 compliant form of ETH back to native ETH that
can be used for gas and other native purposes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await universalGenericUnwrapWeth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
