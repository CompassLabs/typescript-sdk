/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tokenTokenBalance } from "../../funcs/tokenTokenBalance.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1TokenBalanceRequest$inboundSchema,
};

export const tool$tokenTokenBalance: ToolDefinition<typeof args> = {
  name: "token-token-balance",
  description: `Token Balance

Returns the balance of a specific ERC20 token for a given user address.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tokenTokenBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
