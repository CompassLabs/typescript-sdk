/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pendlePendlePositions } from "../../funcs/pendlePendlePositions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1PendlePositionsRequest$inboundSchema,
};

export const tool$pendlePendlePositions: ToolDefinition<typeof args> = {
  name: "pendle-pendle-positions",
  description: `List User's Market Positions

List the user's SY, PT, YT and LP positions for all markets on a given chain.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pendlePendlePositions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
