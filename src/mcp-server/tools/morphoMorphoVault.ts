/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { morphoMorphoVault } from "../../funcs/morphoMorphoVault.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1MorphoVaultRequest$inboundSchema,
};

export const tool$morphoMorphoVault: ToolDefinition<typeof args> = {
  name: "morpho-morpho-vault",
  description: `Get Vault & User Position

Get Vault data & User Position.

The user position is only included if 'user_address' parameter is included.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await morphoMorphoVault(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
