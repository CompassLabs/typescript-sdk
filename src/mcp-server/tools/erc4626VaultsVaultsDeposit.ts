/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { erc4626VaultsVaultsDeposit } from "../../funcs/erc4626VaultsVaultsDeposit.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.VaultDepositRequest$inboundSchema,
};

export const tool$erc4626VaultsVaultsDeposit: ToolDefinition<typeof args> = {
  name: "ERC-4626-vaults-vaults-deposit",
  description: `Deposit to Vault

Deposit tokens into a Vault (ERC-4626 Standard) to earn passive yield.

Each vault accepts one unique token that can be deposited.
                    <Info>
                    **Required Allowances**

                        In order to make this transaction, token allowances need to be set for the following contracts.

                     - \`<vault_address>\`
                    </Info>
                `,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await erc4626VaultsVaultsDeposit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
