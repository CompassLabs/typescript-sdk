/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { morphoMorphoMarket } from "../../funcs/morphoMorphoMarket.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1MorphoMarketRequest$inboundSchema,
};

export const tool$morphoMorphoMarket: ToolDefinition<typeof args> = {
  name: "morpho-morpho-market",
  description: `Get Market

Get data & metrics for a specific Morpho market.

Including:
- Current, daily, weekly, monthly, yearly APY
- Collateral & loan asset data
- Liquidation loan-to-value ratio
- Collateral, borrow & liquidity value
- Utilization ratio
- Pertinent metadata
- Whitelist status`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await morphoMorphoMarket(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
