/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { smartAccountSmartAccountBatchedUserOperations } from "../../funcs/smartAccountSmartAccountBatchedUserOperations.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.BatchedUserOperationsRequest$inboundSchema,
};

export const tool$smartAccountSmartAccountBatchedUserOperations: ToolDefinition<
  typeof args
> = {
  name: "smart-account-smart-account-batched-user-operations",
  description: `Get Smart Account Batched User Operations

Generate a list of user operations for smart account batching.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await smartAccountSmartAccountBatchedUserOperations(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
