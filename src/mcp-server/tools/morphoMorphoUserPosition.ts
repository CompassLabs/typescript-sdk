/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { morphoMorphoUserPosition } from "../../funcs/morphoMorphoUserPosition.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1MorphoUserPositionRequest$inboundSchema,
};

export const tool$morphoMorphoUserPosition: ToolDefinition<typeof args> = {
  name: "morpho-morpho-user-position",
  description: `Check User Position

Check user's overall position across the entire Morpho ecosystem.

Inlcuding all vault and market position metrics and relavant metadata of said vaults
and markets.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await morphoMorphoUserPosition(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
