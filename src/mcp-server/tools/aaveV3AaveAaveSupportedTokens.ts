/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aaveV3AaveAaveSupportedTokens } from "../../funcs/aaveV3AaveAaveSupportedTokens.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1AaveAaveSupportedTokensRequest$inboundSchema,
};

export const tool$aaveV3AaveAaveSupportedTokens: ToolDefinition<typeof args> = {
  name: "aave-v3-aave-aave-supported-tokens",
  description: `Aave Supported Tokens

Returns the list of supported tokens on Aave for the specified network, along
with key metadata.

For each token, the response includes:
- The symbol and contract address.
- Whether the token is currently enabled for supplying (depositing).
- Whether it is enabled for borrowing.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aaveV3AaveAaveSupportedTokens(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
