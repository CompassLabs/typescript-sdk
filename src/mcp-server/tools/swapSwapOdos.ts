/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { swapSwapOdos } from "../../funcs/swapSwapOdos.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.OdosSwapRequest$inboundSchema,
};

export const tool$swapSwapOdos: ToolDefinition<typeof args> = {
  name: "swap-swap-odos",
  description: `Odos Swap

Swap between two tokens using Odos Smart Order Routing.
                    <Info>
                    **Required Allowances**

                        In order to make this transaction, token allowances need to be set for the following contracts.

                     - \`OdosRouter\`
                    </Info>
                `,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await swapSwapOdos(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
