/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { universalGenericSupportedChains } from "../../funcs/universalGenericSupportedChains.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1GenericSupportedChainsRequest$inboundSchema,
};

export const tool$universalGenericSupportedChains: ToolDefinition<typeof args> =
  {
    name: "universal-generic-supported-chains",
    description: `List Supported Chains

Get the list of supported chains by the Compass API.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await universalGenericSupportedChains(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
