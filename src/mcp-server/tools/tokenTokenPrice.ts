/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tokenTokenPrice } from "../../funcs/tokenTokenPrice.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V1TokenPriceRequest$inboundSchema,
};

export const tool$tokenTokenPrice: ToolDefinition<typeof args> = {
  name: "token-token-price",
  description: `Token Price

Retrieves the price of a token in USD.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tokenTokenPrice(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
